/**
 * DeliveryProofService — AWS S3 photo upload
 * AGENT-BACKEND-API is responsible for the full implementation.
 * See /docs/ARCHITECTURE.md § 3.4 for specification.
 *
 * Responsibilities:
 *   - Upload 2 photos per delivery (merchandise + stamped BL)
 *   - Generate signed S3 URLs (24h expiry)
 *   - Store URLs + metadata in delivery_proofs table
 */

// TODO (AGENT-BACKEND-API): Implement delivery proof service

export {};
