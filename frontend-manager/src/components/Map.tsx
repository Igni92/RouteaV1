/**
 * Map — Mapbox GL JS real-time map
 * AGENT-FRONTEND is responsible for the full implementation.
 * Features:
 *   - Driver markers (colored by status)
 *   - Real-time position updates via WebSocket
 *   - Click driver → details panel
 *   - Delivery stop markers
 * See /docs/MANAGER_UI.md for complete specification.
 */

// TODO (AGENT-FRONTEND): Implement Mapbox map component

export {};
